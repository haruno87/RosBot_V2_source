#---standard pioneer footprint---
#---(in meters)---
robot_radius: 0.2
footprint_padding: 0.00

transform_tolerance: 0.2
map_type: costmap

always_send_full_costmap: true

obstacle_layer:
 enabled: true
 obstacle_range: 6.0
 raytrace_range: 6.0
 inflation_radius: 0.6
 track_unknown_space: true
 combination_method: 1

 observation_sources: laser_scan_sensor 
 laser_scan_sensor: {sensor_frame: aft_mapped_2d,data_type: LaserScan, topic: /scan, marking: true, clearing: true}



inflation_layer:
  enabled:              true
  cost_scaling_factor:  10.0  # exponential rate at which the obstacle cost drops off (default: 10)
  inflation_radius:     0.5  # max. distance from an obstacle at which costs are incurred for planning paths.

static_layer:
  enabled:              truea
  map_topic:            "/map"

# #地图类型
# map_type: costmap
# #导航包所需要的传感器
# observation_sources: scan
# #对传感器的坐标系和数据进行配置。这个也会用于代价地图添加和清除障碍物。例如，你可以用激光雷达传感器用于在代价地图添加障碍物，再添加kinect用于导航和清除障碍物。
# #sensor_frame should be your own frame in tftree
# scan: {sensor_frame: aft_mapped_2d, data_type: LaserScan, topic: /scan, marking: true, clearing: true}